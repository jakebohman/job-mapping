# The DuckDB file is rebuilt in CI, never committed (see PROJECT.md flagged risks).
*.duckdb
*.duckdb.wal

# Secrets / API keys
.env
.env.*

# Phase 0 spike output (throwaway measurements)
spike_results/

# Python
__pycache__/
*.pyc
.venv/
venv/

# Node / frontend
node_modules/
dist/

# Panel resumable cache (regenerated)
site/data/_mix_cache.json
